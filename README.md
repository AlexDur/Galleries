# To run the projects in this repo you will need to install one or both two packages depending on the project.

1. Material UI
npm install @mui/material @emotion/react @emotion/styled

2. React player
npm react-player
